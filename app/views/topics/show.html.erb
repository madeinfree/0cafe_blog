<% content_for :banner do %>
  <div id="submenu">
    <h4><%= link_to("返回上層",topics_url) %></h4>
  </div><P>
<% end %>
<%= mkdown_to_html(@topic.context).html_safe %>
<% @comments.each do |msg| %>
<%= msg.title %>
<%= msg.content %>
<% end %>
<h4><%= link_to("Comment",new_topic_comment_path(@topic)) %></h4>
